export const publicPaths = ['/', '/home', '/rules', '/faq', '/design', '/auth'];

// For the test-video page: allows the phone's LAN-origin HMR websocket request through in dev.
// Remove alongside the rest of test-video when that's deleted.
export const devOrigins = [''];
